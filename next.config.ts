import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Uncomment below if you load images from an external domain */
  // images: {
  //   remotePatterns: [{ hostname: "example.com" }],
  // },
};

export default nextConfig;
